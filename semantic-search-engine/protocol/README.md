# Protocol buffer files

This folder contains [Protocol Buffers](https://protobuf.dev/) files used to define protocols.

## Code generation

In order to generate the mode for the servers and clients, run the following command:

```sh
./proto-gen.sh
```