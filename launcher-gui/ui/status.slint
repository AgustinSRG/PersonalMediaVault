
// Status of the launcher
export enum LauncherStatus { closed, opening, open-error, open, create-ask, lock-ask, creating, fatal-error }

// Type of fatal error
export enum FatalErrorType { unknown, ffmpeg-missing, ffprobe-missing }

// Type of open error
export enum OpenErrorType { unknown, create-folder-error, lock-error }
